# syntax=docker/dockerfile:1.6

ARG BASE_IMAGE=alpha-mini-base:latest
FROM ${BASE_IMAGE}

WORKDIR /app

ENV PYTHONPATH=/app

CMD ["poetry", "run", "uvicorn", "services.api.main:app", "--host", "0.0.0.0", "--port", "8000"]
